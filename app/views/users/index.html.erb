<ol>
  <% @users.each do |user| %>
    <li>
      <%= user.email %> |
      <%= link_to "edit", edit_user_url(user) %> |
      <%= link_to "delete", user_url(user), method: :delete, confirm: "Are you sure?" %>
    </li>
  <% end %>
</ol>